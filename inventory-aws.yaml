---
config:
  ssh:
    host-key-check: false

targets:
  - name: osp
    config:
      transport: ssh
      ssh:
        host: 10.250.39.41
        user: ubuntu
        run-as: root
  - name: prometheus
    config:
      transport: ssh
      ssh:
        host: 10.250.39.79
        user: ubuntu
        run-as: root
